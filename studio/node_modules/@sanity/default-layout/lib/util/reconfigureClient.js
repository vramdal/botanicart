'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = reconfigureClient;

var _client = require('part:@sanity/base/client');

var _client2 = _interopRequireDefault(_client);

var _spaces = require('./spaces');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function reconfigureClient(routerState) {
  const space = _spaces.CONFIGURED_SPACES.find(sp => sp.name === routerState.space);
  if (space && space.api) {
    _client2.default.config(space.api);
  }
}